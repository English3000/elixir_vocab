defmodule ElixirVocab.Any do
  @moduledoc ~S"""
  Functions that accept any type for arguments.

  Conversions (2) + Data (2)

  ## Conversions (2)
    * `Kernel`: `to_charlist/1`, `to_string/1` -- don't work for maps

  ## Data (2)
    * `Kernel`: `!=/2`, `==/2`
  """
end
